<IfModule mod_rewrite.c>
    RewriteEngine On

    # Requests die al in /public zitten niet nogmaals herschrijven.
    RewriteRule ^public/ - [L]

    RewriteRule ^$ public/index.php [L]
    RewriteRule (.*) public/$1 [L]
</IfModule>